name: teacher_assist
description: LehrerAssistent – Flutter UI für OpenClaw
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^2.4.0
  shared_preferences: ^2.2.0
  connectivity_plus: ^5.0.0
  flutter_local_notifications: ^17.0.0
  path_provider: ^2.1.0
  intl: ^0.19.0
  file_picker: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
